import { ReadingScreenContent } from '../../../components/ReadingScreen';

export default function BellineTab() {
  return <ReadingScreenContent historyHref="/(tabs)/belline/history" type="free" />;
}
